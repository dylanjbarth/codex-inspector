import React from 'react'
import {createReviewPlan,fetchReview,fetchReviews,launchReview,type FilterOptions,type ReviewDetail,type ReviewPage,type ReviewPlan,type ReviewPlanRequest} from './api'
import {navigate} from './navigation'

type Props={revision:number;projects:FilterOptions['projects']}
const pathParts=()=>location.pathname.split('/').filter(Boolean).map(decodeURIComponent)
const go=navigate
const stamp=(value:string)=>new Date(value).toLocaleString()
const label=(value:string)=>value.replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase())
const reviewRubric=[
  {key:'task_framing_and_steering',title:'Task framing & steering',description:'Goals, constraints, decomposition, corrections, and success criteria.'},
  {key:'execution_efficiency',title:'Execution efficiency',description:'Context, tokens, models, reasoning, tools, retries, and rework.'},
  {key:'delegation_and_workflow',title:'Delegation & workflow',description:'Subagent use, parallelism, coordination, and handoffs.'},
  {key:'reusable_leverage',title:'Reusable leverage',description:'Patterns suited to instructions, skills, hooks, or automation.'},
]
const reviewModels=[
  {value:'gpt-5.6-sol',label:'GPT-5.6 Sol'},
  {value:'gpt-5.6-terra',label:'GPT-5.6 Terra'},
  {value:'gpt-5.6-luna',label:'GPT-5.6 Luna'},
]

export function Reviews({revision,projects}:Props){
  const [,rerender]=React.useState(0)
  React.useEffect(()=>{const update=()=>rerender(x=>x+1);addEventListener('popstate',update);return()=>removeEventListener('popstate',update)},[])
  const parts=pathParts()
  if(parts[1]==='new')return <ReviewPlanner revision={revision} projects={projects}/>
  if(parts[1])return <ReviewReport reviewId={parts[1]}/>
  return <ReviewHistory/>
}

function Shell({children}:{children:React.ReactNode}){return <section className="reviews-shell"><header><div><span className="eyebrow">PERSISTED CODEX TASKS</span><h1>Effectiveness Reviews</h1></div></header>{children}</section>}

function ReviewRubric({compact=false}:{compact?:boolean}){
  if(compact)return <div className="review-summary-card review-rubric-summary"><p className="section-label">FIXED REVIEW LENSES</p><h2>Review rubric</h2><ol>{reviewRubric.map(item=><li key={item.key}>{item.title}</li>)}</ol><p>No maturity level, health score, or forced finding quota.</p></div>
  return <section className="review-rubric-panel" aria-labelledby="review-rubric-title"><header><p className="section-label">ONE VISIBLE EFFECTIVENESS REVIEW</p><h2 id="review-rubric-title">What Codex reviews</h2><p>The rubric guides investigation without assigning a score or forcing findings into rigid categories.</p></header><div className="review-rubric-grid">{reviewRubric.map((item,index)=><div className="review-rubric-item" key={item.key}><strong>{index+1} · {item.title}</strong><p>{item.description}</p></div>)}</div></section>
}

function ReviewHistory(){
  const [page,setPage]=React.useState<ReviewPage|null>(null),[error,setError]=React.useState('')
  React.useEffect(()=>{let live=true;fetchReviews().then(x=>{if(live)setPage(x)}).catch(e=>{if(live)setError((e as Error).message)});return()=>{live=false}},[])
  const more=async()=>{if(!page?.nextCursor)return;const next=await fetchReviews(page.nextCursor);setPage({...next,items:[...page.items,...next.items]})}
  return <Shell><section className="review-landing-header"><div><h2>Evidence-linked guidance, kept local</h2><p>Ask Codex to examine bounded session evidence and return a few concrete strengths and improvements.</p></div><button onClick={()=>go('/reviews/new')}>New review</button></section><div className="review-stack"><ReviewRubric/><section className="review-history-panel" aria-labelledby="review-history-title"><header><div><p className="section-label">SAVED LOCALLY</p><h2 id="review-history-title">Review history</h2><p>Opening a saved review never starts Codex or transmits indexed evidence.</p></div></header>{error?<div className="error"><h2>History unavailable</h2><p>{error}</p></div>:!page?<p className="loading">Discovering local Review artifacts…</p>:<div className="review-history">{page.items.map(item=><article key={item.reviewId}><div><span className={`review-status ${item.status}`}>{label(item.status)}</span><h3>{item.reviewId}</h3><p>{stamp(item.createdAt)}</p></div><button onClick={()=>go(`/reviews/${encodeURIComponent(item.reviewId)}`)}>Open report</button></article>)}{page.items.length===0&&<div className="empty"><h2>No Reviews yet</h2><p>Create a plan, inspect exactly what Codex will receive, then explicitly confirm the launch.</p></div>}{page.nextCursor&&<button onClick={()=>void more()}>Load more</button>}</div>}</section></div></Shell>
}

function ReviewPlanner({revision,projects}:Props){
  const params=new URLSearchParams(location.search),prefilled=params.get('session')||''
  const [scopeKind,setScopeKind]=React.useState(prefilled?'single_session':'time_period'),[root,setRoot]=React.useState(prefilled),[days,setDays]=React.useState('7'),[project,setProject]=React.useState(''),[model,setModel]=React.useState('gpt-5.6-sol'),[reasoning,setReasoning]=React.useState('high'),[focus,setFocus]=React.useState('')
  const [plan,setPlan]=React.useState<ReviewPlan|null>(null),[error,setError]=React.useState(''),[busy,setBusy]=React.useState(false)
  const create=async(event:React.FormEvent)=>{event.preventDefault();setBusy(true);setError('');try{const end=new Date(),start=new Date(end.getTime()-Number(days)*86400000);const scope=scopeKind==='single_session'?{kind:'single_session' as const,rootSessionId:root}:{kind:'time_period' as const,start:start.toISOString(),end:end.toISOString(),timezone:Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC',projectId:project||undefined};const request:ReviewPlanRequest={scope,model,reasoningEffort:reasoning,focus:focus||undefined,requestedRevision:revision};setPlan(await createReviewPlan(request))}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
  const launch=async()=>{if(!plan)return;setBusy(true);setError('');try{const started=await launchReview(plan.planId);go(`/reviews/${encodeURIComponent(started.reviewId)}`)}catch(e){setError((e as Error).message);setBusy(false)}}
  const scopeSummary=scopeKind==='single_session'?(root||'Choose a root session'):`Last ${days==='1'?'24 hours':`${days} days`}`
  const projectSummary=scopeKind==='time_period'?(projects.find(option=>option.value===project)?.label||'All projects'):'Complete descendant tree included',modelLabel=reviewModels.find(option=>option.value===model)?.label||model
  return <Shell><section className="review-plan-surface"><header className="review-plan-header"><div><nav className="review-breadcrumb" aria-label="Review breadcrumb"><a href="/reviews">Reviews</a><span aria-hidden="true">/</span><span aria-current="page">New review</span></nav><p className="section-label">EFFECTIVENESS REVIEW PLAN</p><h2>Review before starting Codex</h2><p>Confirm the search parameters and inspect the exact prompt Codex will receive.</p></div><a className="review-cancel" href="/reviews">Cancel</a></header><form className="review-plan-form" onSubmit={event=>void create(event)}><div className="review-plan-layout"><div className="review-plan-main"><section className="review-plan-section" aria-labelledby="review-scope-title"><h2 id="review-scope-title">1 · Scope</h2><div className="review-scope-choice"><label><input type="radio" name="review-scope" value="time_period" checked={scopeKind==='time_period'} onChange={()=>{setScopeKind('time_period');setPlan(null)}}/><span><strong>Time period</strong><small>Ask Codex to search completed work in this period.</small></span></label><label><input type="radio" name="review-scope" value="single_session" checked={scopeKind==='single_session'} onChange={()=>{setScopeKind('single_session');setPlan(null)}}/><span><strong>Single session</strong><small>Ask Codex to investigate one root and its descendants.</small></span></label></div></section>{scopeKind==='single_session'?<div className="review-field"><label htmlFor="review-root-session">Root session ID</label><input id="review-root-session" required value={root} onChange={e=>{setRoot(e.target.value);setPlan(null)}}/><small>The prompt includes this root as the search boundary.</small></div>:<div className="review-scope-controls"><div className="review-field"><label htmlFor="review-period">Time period</label><select id="review-period" value={days} onChange={e=>{setDays(e.target.value);setPlan(null)}}><option value="1">Last 24 hours</option><option value="7">Last 7 days</option><option value="30">Last 30 days</option></select></div><div className="review-field"><label htmlFor="review-project">Project</label><select id="review-project" value={project} onChange={e=>{setProject(e.target.value);setPlan(null)}}><option value="">All projects</option>{projects.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select></div></div>}<section className="review-plan-section" aria-labelledby="review-focus-title"><h2 id="review-focus-title">2 · Optional focus</h2><div className="review-field"><label htmlFor="review-focus">What should Codex pay special attention to?</label><textarea id="review-focus" maxLength={2000} value={focus} onChange={e=>{setFocus(e.target.value);setPlan(null)}} placeholder="For example: Pay special attention to how I delegate research work."/></div><p className="review-coverage-note">Leave blank for the standard four-lens review. No usefulness rating is required.</p></section><details className="review-details"><summary>Advanced model settings</summary><div className="review-scope-controls"><div className="review-field"><label htmlFor="review-model">Model</label><select id="review-model" required value={model} onChange={e=>{setModel(e.target.value);setPlan(null)}}>{reviewModels.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select></div><div className="review-field"><label htmlFor="review-reasoning">Reasoning</label><select id="review-reasoning" value={reasoning} onChange={e=>{setReasoning(e.target.value);setPlan(null)}}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="xhigh">Extra high</option></select></div></div></details></div><aside className="review-plan-summary" aria-labelledby="review-plan-summary-title"><div className="review-summary-card"><p className="section-label">DRY RUN</p><h2 id="review-plan-summary-title">What Codex will search</h2><dl className="review-summary-list"><dt>Scope</dt><dd>{scopeSummary}</dd><dt>Project</dt><dd>{projectSummary}</dd><dt>Index revision</dt><dd>{plan?.appliedRevision??revision}</dd><dt>Evidence</dt><dd>Selected by Codex within the prompt boundary</dd><dt>Output</dt><dd>review.json</dd></dl></div><ReviewRubric compact/><div className="review-summary-card"><p className="section-label">CODEX TASK</p><h2>{modelLabel}</h2><p><strong>{label(reasoning)} reasoning</strong></p><p>Previewing remains local. Starting creates a visible, continuable Codex task using the prompt below.</p></div></aside></div><footer className="review-plan-footer"><p>Build the exact launch prompt. Codex will not start until you separately confirm it.</p><button disabled={busy}>{busy?'Building prompt…':plan?'Refresh Prompt':'Preview Review'}</button></footer></form>{error&&<div className="error"><h2>Review unavailable</h2><p>{error}</p></div>}{plan&&<PlanPreview plan={plan} busy={busy} launch={launch}/>}</section></Shell>
}

function PlanPreview({plan,busy,launch}:{plan:ReviewPlan;busy:boolean;launch:()=>Promise<void>}){
  const review=plan.review,scope=review.scope,scopeText=scope.kind==='single_session'?`Root ${scope.rootSessionId}`:`${stamp(scope.start)} → ${stamp(scope.end)}`
  return <section className="plan-preview"><p className="section-label">3 · VERIFY &amp; CONFIRM</p><h2>Exact prompt preview</h2><div className="review-facts"><dl><dt>Scope</dt><dd>{scopeText}</dd><dt>Project</dt><dd>{review.projectName||'All projects'}</dd><dt>Index revision</dt><dd>{review.indexRevision}</dd><dt>Model / reasoning</dt><dd>{review.model} / {review.reasoning}</dd><dt>Output</dt><dd>reviews/{review.reviewId}/review.json</dd></dl><div><strong>Fixed four-lens rubric</strong><ol>{reviewRubric.map(x=><li key={x.key}>{x.title}</li>)}</ol></div></div><details open><summary>Exact launch prompt</summary><pre>{plan.launchPrompt}</pre></details><div className="consent"><strong>Separate model boundary</strong><p>Starting sends this prompt to a new Codex task. Codex decides how to search the local index within these parameters and may send evidence it reads to your configured model/service.</p><button disabled={busy} onClick={()=>void launch()}>{busy?'Starting persisted task…':'Confirm & start Review'}</button><small>Codex does not start until this confirmation. Recommendations remain copyable text and are never executed by Inspector.</small></div></section>
}

function ReviewReport({reviewId}:{reviewId:string}){
  const [detail,setDetail]=React.useState<ReviewDetail|null>(null),[error,setError]=React.useState(''),[copied,setCopied]=React.useState('')
  const refresh=React.useCallback(()=>fetchReview(reviewId).then(setDetail).catch(e=>setError((e as Error).message)),[reviewId])
  React.useEffect(()=>{void refresh();const timer=setInterval(()=>void refresh(),700);return()=>clearInterval(timer)},[refresh])
  if(error)return <Shell><div className="error"><h2>Review unavailable</h2><p>{error}</p></div></Shell>
  if(!detail)return <Shell><p className="loading">Reading local Review state…</p></Shell>
  const run=detail.run,report=detail.acceptedReport
  return <Shell><div className="review-report-heading"><div><a className="quiet-link" href="/reviews">← Review history</a><p className="section-label">INSPECTOR-ACCEPTED SNAPSHOT</p><h2>{report?.summary||'Review task in progress'}</h2><span className={`review-status ${run.status}`}>{label(run.status)}</span></div>{run.threadId&&<div className="task-handoff"><a href={`codex://threads/${encodeURIComponent(run.threadId)}`}>Open Codex task</a><button onClick={()=>{void navigator.clipboard.writeText(`codex resume ${run.threadId}`);setCopied('resume')}}>Copy resume command</button><code>{run.threadId}</code>{copied==='resume'&&<small>Copied</small>}</div>}</div>{run.status==='running'||run.status==='planned'?<section className="review-progress"><h2>Codex is investigating the selected scope</h2><p>Inspector is monitoring the persisted task and will accept only the first schema-valid review.json with citations inside the selected parameters.</p></section>:run.status==='failed'||run.status==='unrenderable'?<section className="error"><h2>{run.status==='unrenderable'?'Report cannot be rendered':'Review failed'}</h2><p>{run.failureMessage||'The task exited without an accepted report.'}</p><code>{run.failureCode}</code><p>The prompt, run metadata, and malformed artifact remain local for diagnosis.</p></section>:report?<section className="accepted-report"><div className="report-meta"><span>{label(report.scope.kind)}</span><span>{report.model} · {report.reasoning}</span><span>{stamp(report.completedAt)}</span><span>{report.findings.length} findings</span></div>{report.findings.map(finding=><article key={finding.findingId}><div className="finding-title"><span>{label(finding.kind)}</span><h3>{finding.title}</h3><small>{label(finding.lens)} · {label(finding.support)}</small></div><h4>Observation</h4><p>{finding.observation}</p><h4>Impact</h4><p>{finding.impact}</p><h4>Evidence</h4><p>{finding.evidenceSummary}</p><ul className="citations">{finding.citations.map(citation=>{const target=citation.rootSessionId&&citation.turnId?`/context/${encodeURIComponent(citation.rootSessionId)}/turn/${encodeURIComponent(citation.turnId)}?evidence=${encodeURIComponent(citation.evidenceId)}&return=${encodeURIComponent(`/reviews/${reviewId}`)}`:'#';return <li key={citation.evidenceId}><a href={target}>Open cited event</a><span className={citation.availability==='available'?'exact-badge':'unavailable-badge'}>{label(citation.availability)}</span></li>})}</ul><h4>Recommendation</h4><p>{finding.recommendation}</p>{finding.actionPrompt&&<div className="action-prompt"><pre>{finding.actionPrompt}</pre><button onClick={()=>{void navigator.clipboard.writeText(finding.actionPrompt!);setCopied(finding.findingId)}}>Copy action prompt</button>{copied===finding.findingId&&<small>Copied — Inspector did not run it.</small>}</div>}</article>)}</section>:null}</Shell>
}
